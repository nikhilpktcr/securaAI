{
  "name": "secura-web",
  "private": true,
  "version": "0.1.0",
  "description": "Secura live web platform (Vercel)",
  "engines": {
    "node": ">=18"
  },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "start:local": "npm run build && node server.js"
  },
  "dependencies": {
    "@vercel/node": "^5.3.0"
  },
  "devDependencies": {
    "@types/node": "^20.10.0",
    "typescript": "^5.3.0"
  }
}
